<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.transitapp.ui.Routes.RoutesFragment">

    <AutoCompleteTextView
        android:id="@+id/autoCompleteRoutes"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:inputType="text"
        android:completionThreshold="1"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/addButton"
        app:layout_constraintHorizontal_chainStyle="packed"/>

    <Button
        android:id="@+id/addButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add_button_text"
        app:layout_constraintTop_toTopOf="@id/autoCompleteRoutes"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/autoCompleteRoutes"
        app:layout_constraintBottom_toBottomOf="@id/autoCompleteRoutes"/>

    <TextView
        android:id="@+id/preferredRoutesTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/addButton"
        android:textAppearance="?attr/textAppearanceListItem"/>

</androidx.constraintlayout.widget.ConstraintLayout>
